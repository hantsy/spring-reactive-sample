name: spring-reactive-boot-data-mongo-template

on:
  push:
    paths:
      - "boot-data-mongo-template/**"
    branches: [ master ]
  pull_request:
    paths:
      - "boot-data-mongo-template/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: boot-data-mongo-template

    steps:
    - uses: actions/checkout@v6
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven'  
    - name: Build with Maven
      run: ./mvnw -B package --file pom.xml
