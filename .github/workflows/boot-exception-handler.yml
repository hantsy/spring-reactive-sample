name: boot-exception-handler

on:
  push:
    paths:
      - "boot-exception-handler/**"
    branches: [ master ]
  pull_request:
    paths:
      - "boot-exception-handler/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: boot-exception-handler

    steps:
    - uses: actions/checkout@v7
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven' 
    - name: Build with Maven
      run: ./mvnw -B package --file pom.xml

