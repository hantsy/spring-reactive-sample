name: boot-date-neo4j-cypher

on:
  push:
    paths:
      - "boot-date-neo4j-cypher/**"
    branches: [ master ]
  pull_request:
    paths:
      - "boot-date-neo4j-cypher/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: boot-date-neo4j-cypher

    steps:
    - uses: actions/checkout@v6
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven'
    - name: Build with Maven
      run: ./mvnw -B package --file pom.xml

