name: spring-reactive-data-r2dbc-postgresql

on:
  push:
    paths:
      - "data-r2dbc-postgresql/**"
    branches: [ master ]
  pull_request:
    paths:
      - "data-r2dbc-postgresql/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v2.3.1
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: 'maven'
    - name: Build with Maven
      run: |
        cd data-r2dbc-postgresql
        mvn -B package --file pom.xml
