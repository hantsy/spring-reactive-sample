name: boot-data-redis-template

on:
  push:
    paths:
      - "boot-data-redis-template/**"
    branches: [ master ]
  pull_request:
    paths:
      - "boot-data-redis-template/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "boot-data-redis-template"

    steps:
    - uses: actions/checkout@v7
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: 'maven' 
    - name: Build with Maven
      run: ./mvnw clean verify

