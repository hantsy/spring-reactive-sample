name: spring-reactive-boot-neo4j

on:
  push:
    paths:
      - "boot-neo4j/**"
    branches: [ master ]
  pull_request:
    paths:
      - "boot-neo4j/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: boot-neo4j

    steps:
    - uses: actions/checkout@v6
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'  
    - name: Build with Maven
      run: ./mvnw -B package --file pom.xml
